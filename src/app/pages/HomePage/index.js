import React from 'react';
import Search from '../../components/Search/index';

const HomePage = () => {
    return(
        <section className="section asa">
            <Search />
        </section>
    );
};

export default HomePage;
